import { List } from './part-1';

describe('day 1 part', () => {
  it('should sort the list in numerical order', () => {
    const list = new List();
    list.readList();
    // list.getDistance();
    list.getSimilarity();
  });
});
